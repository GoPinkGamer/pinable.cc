# 使用说明

## 🚀 快速部署

### 一键部署脚本

项目包含自动化部署脚本 `deploy.sh`，可以自动完成以下操作：

1. ✅ 复制 `.chatgame/design_iterations` 中的游戏文件到 `games` 目录
2. ✅ 自动生成游戏配置（`assets/js/config.js`）
3. ✅ 更新游戏加载路径
4. ✅ 提交到 Git
5. ✅ 推送到 GitHub（使用指定的 SSH 密钥）

### 使用方法

```bash
# 给脚本添加执行权限（首次使用）
chmod +x deploy.sh

# 运行部署脚本
./deploy.sh
```

### 脚本功能说明

#### 1. 游戏文件复制
脚本会自动将 `.chatgame/design_iterations/` 目录中的所有文件复制到 `games/` 目录。

#### 2. 自动配置生成
脚本会扫描 `games/` 目录中的 HTML 文件，并根据文件名自动生成游戏配置：

- **泡泡龙游戏** (`bubble_*.html`)
  - 标题：🎯 泡泡龙
  - 标签：休闲、益智、射击、经典
  - 难度：简单
  - 操作：鼠标点击

- **青蛙游戏** (`frog_*.html`)
  - 标题：🐸 小青蛙旅行
  - 标签：冒险、平台、休闲、可爱
  - 难度：中等
  - 操作：键盘/触摸

- **其他游戏**
  - 自动从文件名生成标题
  - 默认标签：休闲、益智
  - 默认难度：简单

#### 3. Git 提交和推送
脚本使用以下命令推送到 GitHub：

```bash
GIT_SSH_COMMAND="ssh -i ~/.ssh/id_devops -o IdentitiesOnly=yes" git push -f origin main
```

这会使用 `~/.ssh/id_devops` SSH 密钥进行认证。

## 📁 目录结构

部署后的目录结构：

```
pinable.cc/
├── games/                          # 游戏文件目录（自动生成）
│   ├── bubble_shooter_1.html       # 泡泡龙游戏
│   ├── bubble_shooter_theme.css    # 泡泡龙主题
│   ├── frog_travel_game_1.html     # 青蛙旅行游戏
│   └── frog_game_theme.css         # 青蛙游戏主题
├── assets/
│   └── js/
│       └── config.js               # 自动生成的游戏配置
└── deploy.sh                       # 部署脚本
```

## 🎮 添加新游戏

### 方法 1：使用部署脚本（推荐）

1. 将新游戏 HTML 文件放入 `.chatgame/design_iterations/`
2. 运行部署脚本：
   ```bash
   ./deploy.sh
   ```
3. 脚本会自动：
   - 复制文件到 `games/`
   - 更新配置
   - 提交并推送

### 方法 2：手动添加

1. 将游戏文件复制到 `games/` 目录
2. 编辑 `assets/js/config.js`，添加游戏配置：

```javascript
{
  id: 'your-game-id',
  filename: 'your_game.html',
  title: '🎮 您的游戏',
  description: '游戏描述',
  longDescription: '详细描述',
  tags: ['标签1', '标签2'],
  theme: 'your_game_theme.css',
  thumbnail: 'assets/images/game-thumbnails/your-game.png',
  featured: true,
  difficulty: 'easy',
  players: '单人',
  controls: '键盘/鼠标',
  addedDate: '2026-01-04'
}
```

3. 提交并推送：
   ```bash
   git add .
   git commit -m "Add new game"
   git push
   ```

## 🔧 自定义配置

### 修改 SSH 密钥路径

如果您的 SSH 密钥不在 `~/.ssh/id_devops`，请编辑 `deploy.sh`：

```bash
# 找到这一行
GIT_SSH_COMMAND="ssh -i ~/.ssh/id_devops -o IdentitiesOnly=yes" git push -f origin main

# 修改为您的密钥路径
GIT_SSH_COMMAND="ssh -i ~/.ssh/your_key -o IdentitiesOnly=yes" git push -f origin main
```

### 修改游戏识别规则

编辑 `deploy.sh` 中的游戏识别部分：

```bash
# 在脚本中找到这部分代码
if [[ $filename == *"bubble"* ]]; then
    # 泡泡龙游戏配置
elif [[ $filename == *"frog"* ]]; then
    # 青蛙游戏配置
else
    # 默认配置
fi
```

添加您自己的识别规则。

## 📊 查看部署状态

### 检查 Git 状态
```bash
git status
```

### 查看最近的提交
```bash
git log --oneline -5
```

### 查看远程仓库
```bash
git remote -v
```

## 🌐 访问网站

部署完成后，访问：
- **主域名：** https://pinable.cc
- **GitHub Pages：** https://yourusername.github.io/pinable.cc

## 🐛 故障排除

### 问题 1：SSH 密钥权限错误

**错误信息：**
```
Permission denied (publickey)
```

**解决方案：**
```bash
# 检查密钥权限
ls -la ~/.ssh/id_devops

# 设置正确的权限
chmod 600 ~/.ssh/id_devops
```

### 问题 2：游戏文件未找到

**错误信息：**
```
⚠️  .chatgame/design_iterations 目录不存在
```

**解决方案：**
确保 `.chatgame/design_iterations` 目录存在且包含游戏文件。

### 问题 3：配置生成失败

**症状：** `assets/js/config.js` 内容不正确

**解决方案：**
1. 检查 `games/` 目录中的 HTML 文件
2. 手动编辑 `assets/js/config.js`
3. 或重新运行部署脚本

### 问题 4：推送失败

**错误信息：**
```
fatal: Could not read from remote repository
```

**解决方案：**
1. 检查 SSH 密钥配置
2. 验证 Git 远程仓库地址
3. 确保有推送权限

## 📝 日常维护

### 更新游戏
1. 修改 `.chatgame/design_iterations/` 中的游戏文件
2. 运行 `./deploy.sh`

### 更新样式
1. 修改 `assets/css/` 中的样式文件
2. 提交并推送：
   ```bash
   git add assets/css/
   git commit -m "Update styles"
   git push
   ```

### 更新配置
1. 手动编辑 `assets/js/config.js`
2. 或修改 `deploy.sh` 中的配置生成逻辑
3. 重新运行部署脚本

## 🔄 回滚版本

如果需要回滚到之前的版本：

```bash
# 查看提交历史
git log --oneline

# 回滚到指定提交
git reset --hard <commit-hash>

# 强制推送
GIT_SSH_COMMAND="ssh -i ~/.ssh/id_devops -o IdentitiesOnly=yes" git push -f origin main
```

## 📚 相关文档

- [README.md](README.md) - 项目说明
- [DEPLOYMENT.md](DEPLOYMENT.md) - 详细部署指南
- [TESTING.md](TESTING.md) - 测试指南

## 💡 最佳实践

1. **定期备份** - 在重大更改前创建 Git 标签
2. **测试后部署** - 本地测试通过后再推送
3. **版本控制** - 使用有意义的提交信息
4. **文档更新** - 添加新功能时更新文档

---

**需要帮助？** 查看 [DEPLOYMENT.md](DEPLOYMENT.md) 或提交 Issue。